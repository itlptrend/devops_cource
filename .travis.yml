dist: trusty
sudo: required
language: bash

#before_install:
#  - curl https://raw.githubusercontent.com/express42/otus-homeworks/2019-02/run.sh | bash

before_install:
  - echo before_install
  - ls /
  - cat /etc/issue
  - apt-get update
script:
  - echo script
